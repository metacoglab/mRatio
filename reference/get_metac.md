# Get the parameterization of `meta_c` in `model`

Get the parameterization of `meta_c` in `model`

## Usage

``` r
get_metac(model)
```

## Arguments

- model:

  The `brms` model to get the parameterization for

## Value

A character vector, either `"absolute"` or `"relative"`.
